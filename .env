# RAG: прямой URL (если не задан — запросы идут через Gateway /api/rag/)
VITE_RAG_URL=http://localhost:8000
# AG-UI: относительный путь — через Vite proxy (/api → :8083), без cross-origin CORS
VITE_AG_UI_URL=/api/ag-ui
