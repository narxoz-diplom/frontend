# Копия для справки. Vite загружает переменные только из frontend/.env (корень проекта).
# Скопируйте в frontend/.env и перезапустите npm run dev.
VITE_RAG_URL=http://localhost:8000
VITE_AG_UI_URL=http://localhost:5001
